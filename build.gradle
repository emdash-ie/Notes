buildDir '/Users/Noel/Desktop/Temp'

task buildNotes {
    group 'Build'
    description 'Builds the notes using pandoc'

    doLast {
        FileTree inputs = fileTree(dir: '.', include: '**/*.md')
        inputs.each {File file ->
            exec {
                executable 'runPandoc'
                args = [
                    file.getAbsolutePath(),
                    projectDir,
                    buildDir
                ]
            }
        }
    }
}
